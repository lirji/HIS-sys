# Vite dev server(pnpm dev,:5173)默认鉴权模式。
# dual = 账号密码 + SSO 一页两入口(与容器 dual 叠加层一致)。
# 改这里后需重启 pnpm dev 生效。password / idp / dual 三选一。
VITE_AUTH_MODE=dual
VITE_OIDC_AUTHORITY=http://localhost:9008
VITE_OIDC_CLIENT_ID=his-web

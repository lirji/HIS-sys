# Grafana 启动时自动装配 Prometheus 数据源(容器内通过服务名 prometheus 互访)。
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

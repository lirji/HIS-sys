# 自动从挂载目录加载看板 JSON,无需手工导入。
apiVersion: 1

providers:
  - name: his-dashboards
    orgId: 1
    folder: HIS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
